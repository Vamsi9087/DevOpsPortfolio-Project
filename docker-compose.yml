version: "3.8"

services:
  web:
    build: .
    container_name: devops-portfolio
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
