services:  # Remove version: "3.8" line completely
  web:
    build: .
    container_name: devops-portfolio
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
